#!/usr/bin/env bash

# Default directories
if [ -d "/x" ]; then
    baseSrc="/x"
elif [ -d "$HOME/x" ]; then
    baseSrc="$HOME/x"
elif [ -d "/root/x" ]; then
    baseSrc="/root/x"
fi

if [ -d "/x.obs/@rish/@.xet/" ]; then
    doxSrc="/x.obs/@rish/@.xet"
elif [ -d "$HOME/x.obs/@rish/@.xet/" ]; then
    doxSrc="$HOME/x.obs/@rish/@.xet"
elif [ -d "${baseSrc}/local/dox/" ]; then
    doxSrc="${baseSrc}/local/dox"
fi

# Define default directories
def_baseDir="$doxSrc"
def_otherDirs=("${baseSrc}/asset" "${baseSrc}/local")
baseDir="${def_baseDir}"
otherDirs=("${def_otherDirs[@]}")

scriptPath="$(readlink -f ${BASH_SOURCE[0]:-$0})"
scriptDir="$(dirname "${scriptPath}")"
scriptName=${0##*/}
source "${scriptDir}/libxlvini"

catSig=0
copySig=0
gistSig=0
openSig=0
pagerSig=0
quietSig=0
showSig=0
searchString=""
usage() {
    cat <<-EOF
    'cat $0'

    Usage: $(basename $0) [options]

    Options:
    -s, string         search string
    -q, quiet         wont print a word on your tidy terminal !!
    -p, preview       opens a preview sidebar as move through files
    -c, copy          copy file contents to clipboard
    -o, open          open file after selection
    -b, basedir DIR   define base directory to search files
    -h, help          display this help message
EOF
}

#- fn to handle file operations
opr() {
    local file="$(realpath "$1")"
    local basefile=$(basename "$file")
    local type="${2:-${basefile##*.}}"

    if (( catSig == 1 )); then
        cat "$file"

    elif [[ -e $file ]]; then

        if [[ -z $type || "$type" == "$basefile" ]]; then
            local mime=$(file --brief --mime-type --dereference "$file")
            local category="${mime%%/*}"
            local type="${mime##*/}"
        fi

        smsg "$(msg ${type:---}) $(inf "| $file\n")"

        case "$type" in
        md)
            $parse "$file"
            ;;
        bash | sh | zsh | ksh | dash | x-shellscript | \
            c | cpp | h | hpp | cs | java | go | rs | swift | ts | \
            js | json | yaml | yml | toml | ini | xml | html | \
            css | scss | py | lua | rb | php | pl | sql | \
            x-shellscript)
            if [[ "$parser" == "glow" ]]; then
                {
                    echo "\`\`\`$type"
                    cat "$file"
                    # echo "\`\`\`"
                } | $parse
            else
                $parse "$file"
            fi
            ;;
        jpg | jpeg | png)
            if command -v imgcat &>/dev/null; then
                imgcat "$file"
            else
                :
                wrn "'imgcat' not found."
            fi
            ;;
        pdf)
            # inf "This is a PDF file. Performing PDF-specific actions..."
            # Add your PDF handling actions here
            ;;
        *)

            cat "$file"
            ;;
        esac
    else
        swrn "File not found '$(inf "$file")'"
    fi
}

#- parse args
while [[ $# -gt 0 ]]; do
    case $1 in
    - | cat)
        catSig=1
        shift
        ;;
    -c | copy)
        copySig=1
        shift
        ;;
    -d | d | dir)
        baseDir="$2"
        shift 2
        ;;
    -o | o | open)
        openSig=1
        shift
        ;;
    -p | p | preview)
        gistSig=1
        # fzf_param+="--preview 'bash -c \"opr {}\"' "
        shift
        ;;
    pg | pager)
        xglow_param+="--pager "
        pagerSig=1
        shift
        ;;
    -q | q | quiet)
        quietSig=1
        shift
        ;;
    -s | s | string)
        string="$2"
        shift 2
        ;;
    -- | view)
        showSig=1
        shift
        ;;
    *\*)
        searchString="${1%%\*}"
        showSig=1
        shift
        ;;
    -h | help)
        usage
        exit 0
        ;;
    *)
        searchString="$1"
        shift
        ;;
    esac
done

#- MAIN->
{
if command -v glow &>/dev/null; then
    parser="glow"
    parse="glow -w 0"
elif command -v xglow &>/dev/null; then
    parser="glow"
    parse="xglow -w 0"
else
    parse="cat"
fi
}


#- export fn's for fzf preview
export -f inf msg smsg wrn alrt out opr
export parse parser scriptName

#- file selection with fzf
validDirs=()
for d in "${otherDirs[@]}"; do
    [[ -d "$d" ]] && validDirs+=("$d")
done

if (( ${#validDirs[@]} == 0 )); then
    err "No valid directories found to search."
    exit 1
fi

# Change dir so docs ($baseDir) file paths dont include parent path
cd "$(dirname "${baseDir}")" || validDirs=("${validDirs[@]}" "${baseDir}")

file="$(find "$(basename "${baseDir}")" "${validDirs[@]}" -name '.git*' -prune -o -type f -print0 2>/dev/null | sort -u | xargs -0 awk '
    BEGIN { OFS="" }
    FNR==1 { title=""; has_title=0; file=FILENAME }
    /^title:[ \t]*/ {
        sub(/^title:[ \t]*/, "");
        title=$0;
        has_title=1;
        print file, "\t", title, "  \033[90m", file, "\033[0m";
        nextfile;
    }
    FNR>10 {
        if (!has_title) {
            n=split(file, a, "/");
            print file, "\t", a[n], "  \033[90m", file, "\033[0m";
        }
        nextfile;
    }
    ENDFILE {
        if (!has_title && FNR<=10) {
            n=split(file, a, "/");
            print file, "\t", a[n], "  \033[90m", file, "\033[0m";
        }
    }
' 2>/dev/null | (
    if (( gistSig )); then
        fzf --delimiter="\t" --with-nth=2.. --ansi --query="${searchString}" --preview 'bash -c "opr {1}"' $fzf_param
    elif (( showSig )); then
        fzf --delimiter="\t" --with-nth=2.. --ansi --filter="${searchString}" | head -n1
    else
        fzf --delimiter="\t" --with-nth=2.. --ansi --query="${searchString}" $fzf_param
    fi
))" &&
    if [[ -n $file ]]; then
        file="${file%%$'\t'*}"
        #- Perform the action based on the openSig flag
        (( quietSig != 1 )) && opr "$file"
        (( openSig == 1 )) && (xdg-open "$file" || wrn "Failed to open '$file'")
        (( copySig == 1 )) && command -v xclip >/dev/null &&
            (xclip -selection clipboard <"$file" && smsg "Copied contents to clipboard $(inf "'$file'")" ||
                swrn "Failed to copy contents of $file") &
    else
        inf "No file selected.."
    fi
